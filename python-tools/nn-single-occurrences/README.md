# Neural Network

Small expandable Keras model with one ReLU hidden layer, described declaratively.

## Files

- `neural_network/model.py`: defines the network
- `example.py`: runs a tiny forward pass
- `requirements.txt`: dependency list

## Run

```bash
cd neural-network
pip install -r requirements.txt
python example.py
```
